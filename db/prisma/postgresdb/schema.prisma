// db/prisma/postgres/schema.prisma
generator client {
  provider = "prisma-client-js"
  output   = "../../../generated/prisma/postgres/client"
}

datasource postgres {
  provider = "postgresql"
  url      = env("POSTGRES_URL")
}

// Define your PostgreSQL models here